# Project Rules

## Security & Privacy Rules
- **NEVER READ `.env` FILES**: Tuyệt đối không bao giờ được sử dụng bất kỳ công cụ nào (`view_file`, `read_file`, script, v.v.) để đọc file `.env` hoặc bất kỳ file cấu hình bí mật thực tế nào của dự án.
